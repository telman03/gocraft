package main

import (
	"log"
	"os"
	"github.com/gofiber/fiber/v2"
	
	
	"test-basic/internal/auth"
	"test-basic/internal/db"
	"test-basic/internal/router"
	"test-basic/internal/config"
)

func main() {
	config.Load()
	db.Connect()

	
	// Fiber framework
	app := fiber.New(fiber.Config{
		AppName: "test-basic",
	})
	router.SetupRoutes(app)
	
	port := os.Getenv("PORT")
	if port == "" {
		port = "8080"
	}
	
	log.Printf("Server starting on port %s", port)
	log.Fatal(app.Listen(":" + port))
	
}